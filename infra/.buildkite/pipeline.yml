env:
  CI: true
  TF_IN_AUTOMATION: true

steps:
  - label: ":terraform: Terraform Validate"
    agents:
      queue: "buildkite-agent"
    commands:
      - make validate

  - wait

  - label: ":terraform: Terraform Format"
    agents:
      queue: "buildkite-agent"
    commands:
      - make ci-fmt
